<%= form_with(model: milestone) do |form| %>
  <% if milestone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(milestone.errors.count, "error") %> prohibited this milestone from being saved:</h2>

      <ul>
        <% milestone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="w-50 alert alert-success" id="milestone_edit_form" role="alert">
    <%= form.label "Milestone", style: "display: block" %>
    <%= form.text_field :descriptive_name, class: "bg-success-subtle", :maxlength => "40" %>
    &nbsp &nbsp
    <%= form.label "Due : " %>
    <%= form.date_field :estimated_completion_date %>

    &nbsp &nbsp
    <%= form.button "Update", class: "update_btn btn btn-danger btn-sm", method: :update %>
    &nbsp
    <%= form.button "Cancel", class: "btn btn-outline-success btn-sm" %>

  </div>


<% end %>
